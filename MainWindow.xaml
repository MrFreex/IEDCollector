<Window x:Class="IEDCollector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:IEDCollector"
        xmlns:p="clr-namespace:IEDCollector.Properties"
        mc:Ignorable="d"
        Title="IED Collector" Height="500" Width="800" ResizeMode="CanResizeWithGrip" Closing="handleClosing" SizeToContent="Manual">
    <DockPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Menu DockPanel.Dock="Top">

            <!-- <MenuItem Header="_File"> -->
            <!-- <MenuItem Header="Login" /> -->
            <!-- <MenuItem Header="Load" /> -->
            <!-- <MenuItem Header="Save" /> -->
            <!-- </MenuItem> -->

            <MenuItem Header="{x:Static p:Resources.options}">
                <MenuItem x:Name="menuOpenConfiguration" Header="{x:Static p:Resources.configuration}" Click="openConfiguration"/>
                <MenuItem x:Name="menuOpenPreferences" Header="{x:Static p:Resources.preferences}" Click="openPreferences"/>
            </MenuItem>
            <MenuItem Header="{x:Static p:Resources.about}" Click="openAbout"/>
            <MenuItem x:Name="openLicenseButton" Header="{x:Static p:Resources.license}" Click="openLicenseWindow"/>
        </Menu>
        <Grid DockPanel.Dock="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="80" />
            </Grid.RowDefinitions>
            <TabControl MinHeight="300" x:Name="tabControl" DockPanel.Dock="Top" Grid.Row="0" SelectionChanged="tabChangeHandler">
                <TabItem x:Name="viewerModeTab" Header="{x:Static p:Resources.monitor_tab}">
                    
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TreeView Grid.Column="0" x:Name="iedTree" RenderTransformOrigin="0.5,0.505" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                <TreeView.Background>
                                    <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.MenuColorKey}}"/>
                                </TreeView.Background>
                            </TreeView>
                            <GridSplitter Background="Gray" Grid.Column="1"
                                  Width="5"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  ResizeBehavior="PreviousAndNext"
                                  ResizeDirection="Columns" />
                            <Grid VerticalAlignment="Stretch" Grid.Column="2" Margin="5,0,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                                <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                                    <StackPanel.Resources>
                                        <Style TargetType="{x:Type Button}">
                                            <Setter Property="Margin" Value="0,0,10,0"/>
                                        </Style>
                                    </StackPanel.Resources>
                                    <Button IsEnabled="False" x:Name="startPolling" ToolTip="{x:Static p:Resources.cycle_fetch}" HorizontalAlignment="Left" VerticalAlignment="Top" Click="runPolling">
                                        <Image Source="Icons/arrow-repeat.png" Width="20" Height="20" />
                                    </Button>
                                    <Button IsEnabled="False" x:Name="startSingle" ToolTip="{x:Static p:Resources.single_fetch}" HorizontalAlignment="Left" VerticalAlignment="Top" Click="runSingle">
                                        <Image Source="Icons/play-fill.png" Width="20" Height="20" />
                                    </Button>
                                    <Button IsEnabled="False" x:Name="stop" HorizontalAlignment="Left" VerticalAlignment="Top" Click="Stop">
                                        <Image Source="Icons/stop-fill.png" Width="20" Height="20" />
                                    </Button>
                                <CheckBox x:Name="resumePollingStartup" Content="{x:Static p:Resources.resume_polling_startup}" IsChecked="False" Checked="resumePollingStartup_Checked" Unchecked="resumePollingStartup_Checked"/>
                                </StackPanel>
                                <Grid Grid.Row="1" VerticalAlignment="Stretch" Margin="0,10,10,0" >
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" x:Name="ProgressLabel" Margin="0,0,0,10" />
                                    <ProgressBar Grid.Row="1" x:Name="Progress" Height="21" Value="50" MinWidth="300" />
                                <TextBlock Grid.Row="2" x:Name="FileName" TextWrapping="Wrap" Text="{x:Static p:Resources.no_file_being_processed}" Margin="0,5,0,0"/>
                                <ProgressBar Grid.Row="3" x:Name="FileProgress" Height="21" Margin="0,10,0,0" />
                                <ListBox Grid.Row="4" x:Name="actionsbox" Margin="0,10,0,0"/>
                                </Grid>
                            </Grid>
                        </Grid>
                    

                </TabItem>
                <TabItem VerticalAlignment="Stretch" x:Name="configurationModeTab" Header="{x:Static p:Resources.configuration_tab}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <GroupBox HorizontalAlignment="Stretch" Margin="5" Header="{x:Static p:Resources.profile}" Grid.Row="0">
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />

                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ComboBox SelectionChanged="profileSelector_SelectionChanged" Grid.Row="0" Grid.Column="0" x:Name="profileSelector" IsEnabled="False">
                                    <ComboBox.Items>
                                        <ComboBoxItem IsSelected="True" Content="{x:Static p:Resources.no_profile}">

                                        </ComboBoxItem>
                                    </ComboBox.Items>
                                </ComboBox>

                                <Grid HorizontalAlignment="Right" Grid.Row="0" Grid.Column="2" VerticalAlignment="Top">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="300" />
                                        <ColumnDefinition Width="50" />
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Column="0" VerticalAlignment="Center" Content="{x:Static p:Resources.root_folder}"/>
                                    <TextBox Grid.Column="1" VerticalAlignment="Center" Margin="5,0,5,0" Height="25" MinWidth="140" x:Name="rootFolderInput" TextChanged="profileFieldChanged" />
                                    <Button Grid.Column="2" VerticalAlignment="Center" Padding="5,0,5,0" Height="25" Content="Browse" Click="BrowseProfileRootFolder"/>
                                </Grid>
                                <Grid HorizontalAlignment="Stretch" Grid.Row="1" Grid.Column="2" VerticalAlignment="Top">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="300" />
                                        <ColumnDefinition Width="50" />
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Column="0" VerticalAlignment="Center" Content="{x:Static p:Resources.polling_interval}" />
                                    <xctk:IntegerUpDown Minimum="1" Maximum="60" Text="10" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="5,0,0,0" Height="25" MinWidth="140" x:Name="pollingInterval" ValueChanged="profileFieldChanged" />
                                </Grid>
                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5,0,0" Orientation="Horizontal">
                                    <StackPanel.Resources>
                                        <Style TargetType="{x:Type Button}">
                                            <Setter Property="Margin" Value="0,0,10,0" />
                                            <Setter Property="Padding" Value="2" />

                                        </Style>
                                        <Style TargetType="{x:Type Image}">
                                            <Setter Property="Width" Value="22" />
                                        </Style>
                                    </StackPanel.Resources>
                                    <Button Click="createProfile" x:Name="newProfileButton" ToolTip="{x:Static p:Resources.new_profile}">
                                        <Image Source="Icons/database-fill-add.png" />
                                    </Button>
                                    <Button Click="saveProfile" x:Name="saveProfileButton" ToolTip="{x:Static p:Resources.save_profile}">
                                        <Image Source="Icons/database-fill-check.png" />
                                    </Button>
                                    <Button Click="importProfile" x:Name="loadProfileButton" ToolTip="{x:Static p:Resources.import_profile}">
                                        <Image Source="Icons/database-fill-down.png" />
                                    </Button>
                                    <Button x:Name="exportProfileButton" ToolTip="{x:Static p:Resources.export_profile}" Click="exportProfile">
                                        <Image Source="Icons/database-fill-up.png" />
                                    </Button>
                                    <Button Click="renameProfile" x:Name="renameProfileButton" ToolTip="{x:Static p:Resources.rename_profile}">
                                        <Image Source="Icons/database-fill-gear.png" />
                                    </Button>
                                    <Button Click="deleteProfile" x:Name="deleteProfileButton" ToolTip="{x:Static p:Resources.delete_profile}">
                                        <Image Source="Icons/database-fill-x.png" />
                                    </Button>
                                </StackPanel>
                            </Grid>

                        </GroupBox>
                        <GroupBox HorizontalAlignment="Stretch" Header="{x:Static p:Resources.connections}" Grid.Row="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <ListBox Grid.Row="0" x:Name="iedSelector" SelectionChanged="iedSelector_SelectionChanged">
                                    <ListBox.Background>
                                        <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.MenuColorKey}}"/>
                                    </ListBox.Background>
                                </ListBox>
                                <Button Grid.Row="1" HorizontalAlignment="Stretch" ToolTip="{x:Static p:Resources.fetch_data_tooltip}" x:Name="connectIedInConf" Margin="0,5,0,5" IsEnabled="False" Click="fetchIedData">
                                    <Button.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <Image VerticalAlignment="Center" Source="Icons/arrow-repeat.png" Width="20" />
                                            <TextBlock x:Name="fetchDataText" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0,10,0" Text="{x:Static p:Resources.fetch_data}" />
                                        </StackPanel>
                                    </Button.Content>
                                </Button>
                                
                                <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0,5,0,0" Orientation="Horizontal">
                                    <StackPanel.Resources>
                                        <Style TargetType="{x:Type Button}">
                                            <Setter Property="Margin" Value="0,0,10,0"/>
                                        </Style>

                                    </StackPanel.Resources>

                                    <Button x:Name="newIedBtn" ToolTip="{x:Static p:Resources.new_connection}" Click="addIed">
                                        <Image Width="20" Source="Icons/journal-plus.png"/>
                                    </Button>
                                    <Button x:Name="cloneIedBtn" ToolTip="{x:Static p:Resources.clone_connection}" Click="cloneIed">
                                        <Image Width="20" Source="Icons/journals.png"/>
                                    </Button>
                                    <Button x:Name="deleteIedBtn" ToolTip="{x:Static p:Resources.delete_connection}" Click="deleteIed">
                                        <Image Width="20" Source="Icons/journal-minus.png"/>
                                    </Button>
                                    <Button ToolTip="{x:Static p:Resources.save_connection}" x:Name="saveIED" IsEnabled="False" Click="saveIed">
                                        <Image VerticalAlignment="Center" Source="Icons/journal-check.png" Width="20" />
                                    </Button>
                                </StackPanel>

                                <Grid x:Name="iedSettings" Grid.Column="2" Grid.Row="0" Grid.RowSpan="3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="20" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="1" Grid.Column="0">
                                        <StackPanel.Resources>
                                            <Style TargetType="{x:Type Label}">
                                                <Setter Property="HorizontalAlignment" Value="Stretch" />
                                            </Style>
                                            <Style TargetType="{x:Type TextBox}">
                                                <Setter Property="HorizontalAlignment" Value="Stretch" />
                                            </Style>
                                            <Style TargetType="{x:Type ComboBox}">
                                                <Setter Property="HorizontalAlignment" Value="Stretch" />
                                            </Style>
                                            <Style TargetType="{x:Type ListBox}">
                                                <Setter Property="HorizontalAlignment" Value="Stretch" />
                                            </Style>

                                        </StackPanel.Resources>
                                        <Label Content="{x:Static p:Resources.connection_name}" HorizontalAlignment="Stretch" />
                                        <TextBox HorizontalAlignment="Stretch" x:Name="iedNameInput" TextChanged="updateNameLabel" PreviewTextInput="GenericTextBox_PreviewTextInput"  />

                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="10" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Label Grid.Row="0" Grid.Column="0" HorizontalAlignment="Stretch" Content="{x:Static p:Resources.protocol}" />
                                            <ComboBox Grid.Row="1" Grid.Column="0">
                                                <ComboBox.Items>
                                                    <ComboBoxItem IsSelected="True" Content="IEC61850" />
                                                    <ComboBoxItem Visibility="Hidden" Content="FTP" IsEnabled="False" />
                                                    <ComboBoxItem Visibility="Hidden" Content="SFTP" IsEnabled="False" />
                                                    <ComboBoxItem Visibility="Hidden" Content="{x:Static p:Resources.direct_protocol}" IsEnabled="False" />
                                                </ComboBox.Items>
                                            </ComboBox>
                                            <Label Grid.Row="0" Grid.Column="2" Content="{x:Static p:Resources.ip_address}" />
                                            <TextBox Grid.Row="1" Grid.Column="2" x:Name="iedIpInput"  LostFocus="validateIedIP" TextChanged="iedFieldChanged" PreviewTextInput="ipFormatting" />
                                            <Label Grid.Row="2" Grid.Column="0" Content="{x:Static p:Resources.port}" />
                                            <xctk:IntegerUpDown Grid.Row="3" Grid.Column="0" Minimum="0" Maximum="65353" x:Name="iedPortInput" ValueChanged="iedFieldChanged" />
                                            <Label Grid.Row="2" Visibility="Hidden" Grid.Column="2" Content="{x:Static p:Resources.encryption}" />
                                            <ComboBox Visibility="Hidden" Grid.Row="3" Grid.Column="2">
                                                <ComboBox.Items>
                                                    <ComboBoxItem IsSelected="True" Content="{x:Static p:Resources.no_encryption_choice}" />
                                                </ComboBox.Items>
                                            </ComboBox>
                                            <Label Visibility="Hidden" Grid.Row="4" Grid.Column="0" Content="{x:Static p:Resources.username}" />
                                            <TextBox Visibility="Hidden" Grid.Row="5" Grid.Column="0" x:Name="iedUsernameInput"  LostFocus="validateIedIP" TextChanged="iedFieldChanged" />
                                            <Label Visibility="Hidden" Grid.Row="4" Grid.Column="2" Content="{x:Static p:Resources.password}" />
                                            <TextBox Visibility="Hidden" Grid.Row="5" Grid.Column="2" x:Name="iedPasswordInput"  LostFocus="validateIedIP" TextChanged="iedFieldChanged" />
                                        </Grid>


                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="2">
                                        <Label Content="{x:Static p:Resources.subfolder}" />
                                        <TextBox x:Name="iedLogFolderInput" TextChanged="iedFieldChanged" PreviewTextInput="GenericTextBox_PreviewTextInput" />
                                        <GroupBox Header="{x:Static p:Resources.source_filter}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="2*" />
                                                    <ColumnDefinition Width="5" />
                                                    <ColumnDefinition Width="1.5*" />
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>

                                                <Grid Grid.Row="0" Grid.Column="0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <Label Grid.Column="0" Content="{x:Static p:Resources.folders}" />
                                                    <Button x:Name="addFolderButton" Click="addFolderButton_Click" VerticalAlignment="Center" Grid.Column="1">
                                                        <Image Width="15" Source="Icons/plus-lg.png"/>
                                                    </Button>
                                                    <Button Margin="2.5,0,0,0" x:Name="delFolderButton" Click="delFolderButton_Click" VerticalAlignment="Center" Grid.Column="2">
                                                        <Image Width="15" Source="Icons/dash.png"/>
                                                    </Button>
                                                </Grid>
                                                <ListBox Grid.Row="1" Grid.Column="0" x:Name="iedLogFoldersIncludedInput" Height="80" d:ItemsSource="{d:SampleData ItemCount=5}"/>
                                                <Grid Grid.Row="0" Grid.Column="2">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <Label Grid.Column="0" Content="{x:Static p:Resources.extensions}" />
                                                    <Button x:Name="addFileExtensionButton" Click="addFileExtensionButton_Click" VerticalAlignment="Center" Grid.Column="1">
                                                        <Image Width="15" Source="Icons/plus-lg.png"/>
                                                    </Button>
                                                    <Button Margin="2.5,0,0,0" x:Name="delFileExtensionButton" Click="delFileExtensionButton_Click" VerticalAlignment="Center" Grid.Column="2">
                                                        <Image Width="15" Source="Icons/dash.png"/>
                                                    </Button>
                                                </Grid>
                                                
                                                <ListBox Grid.Row="1" Grid.Column="2" x:Name="iedLogExtensionsIncludedInput" Height="80" d:ItemsSource="{d:SampleData ItemCount=5}"/>
                                            </Grid>
                                        </GroupBox>


                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </GroupBox>
                    </Grid>
                </TabItem>
            </TabControl>
            <GridSplitter Background="Gray" Margin="0,5,0,5" Grid.Row="1"
                                  Height="5"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  ResizeBehavior="PreviousAndNext"
                                  ResizeDirection="Rows" />
            <TextBox IsUndoEnabled="False" Grid.Row="2" DockPanel.Dock="Top" HorizontalScrollBarVisibility="Visible"  VerticalScrollBarVisibility="Visible" x:Name="Logs" MinHeight="80" TextChanged="scrollToBottom" />

        </Grid>
    </DockPanel>
</Window>
