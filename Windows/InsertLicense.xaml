<Window x:Class="IEDCollector.Windows.InsertLicense"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IEDCollector.Windows"
        mc:Ignorable="d"
        Closing="Window_Closing"
        Title="License Management" MinHeight="200" MinWidth="400" ResizeMode="CanResize" SizeToContent="WidthAndHeight">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid HorizontalAlignment="Stretch" Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" x:Name="Description" />
            <Label Grid.Column="0" Grid.Row="1">License Key</Label>
            <PasswordBox Grid.Column="0" Width="200" x:Name="licenseBox"  Grid.Row="2"  HorizontalAlignment="Left" />
            <Button Click="Copy_Click" Margin="5,0,0,0" Padding="5,2.5,5,2.5" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Stretch">
                Copy
            </Button>
        </Grid>

        <Label Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right">
            <Hyperlink Click="requestLink_Click" x:Name="requestLink">
                Request License
            </Hyperlink>
        </Label>
        <Button Click="Submit_Click" Grid.Column="1" Margin="5" Padding="5" Grid.Row="1" HorizontalAlignment="Right">Submit</Button>
        <Button x:Name="removeLicenseButton" Click="Remove_Click" Grid.Column="2" Margin="5" Padding="5" Grid.Row="1" HorizontalAlignment="Right">Remove License</Button>
    </Grid>
</Window>
