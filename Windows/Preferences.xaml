<Window x:Class="IEDCollector.Preferences"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IEDCollector"
        xmlns:p="clr-namespace:IEDCollector.Properties"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.preferences_window}" Height="200" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,0,5" Text="{x:Static p:Resources.language}"/>
        <ComboBox x:Name="languageCombo" Grid.Row="1">

        </ComboBox>
        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Grid.Row="3">
            <Button Padding="5" Margin="0,0,5,0" Click="Save_Click" Content="{x:Static p:Resources.save}" />
            <Button Padding="5" Margin="0,0,5,0" Click="Cancel_Click" Content="{x:Static p:Resources.cancel}" />
        </StackPanel>
    </Grid>
</Window>
