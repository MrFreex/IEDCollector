<Window x:Class="IEDCollector.Configuration"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:IEDCollector"
        xmlns:p="clr-namespace:IEDCollector.Properties"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.configuration}" Height="154" Width="509"
        ResizeMode="CanResizeWithGrip" SizeToContent="WidthAndHeight"
        >

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Label HorizontalAlignment="Left" Margin="0,0,5,0" Content="{x:Static p:Resources.log_level}" />
                <ComboBox Margin="0,0,5,0" Grid.Column="1" VerticalAlignment="Center" x:Name="logLevels">

                </ComboBox>
            </Grid>
            <CheckBox Grid.Column="1" Grid.Row="0" x:Name="keepAllLogFiles" Margin="5" Content="{x:Static p:Resources.keep_all_files}" HorizontalAlignment="Center" VerticalAlignment="Center" Checked="keepAllFiles_Checked" Unchecked="keepAllFiles_Checked"/>
            <Grid Grid.Column="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Label VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Column="0" Content="{x:Static p:Resources.log_files_kept}" />
                <TextBox MinWidth="50" Margin="5" Grid.Column="1" x:Name="logFilesKept" VerticalAlignment="Center" HorizontalAlignment="Stretch" Width="Auto" />
                <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="{x:Static p:Resources.files}" />
            </Grid>


            <TextBlock Margin="10,5,10,5" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" Text="{x:Static p:Resources.software_data_location}" />
            <TextBox Grid.Row="1" Grid.Column="1" x:Name="dataLocation" Margin="10,5,10,5" />
            <Button Grid.Row="1" Grid.Column="2" Click="browseDataLocation" Margin="5" HorizontalAlignment="Center" Padding="10,0,10,0" Content="{x:Static p:Resources.browse}" />
            <CheckBox Grid.Row="2" x:Name="startWithWindows" Content="{x:Static p:Resources.start_with_windows}" />
            <CheckBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" x:Name="minimizeToTray" Content="{x:Static p:Resources.minimize_to_tray_on_close}" />
            
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Click="save" Padding="10,0,10,0" HorizontalAlignment="Right" Margin="5" Grid.Column="1" Content="{x:Static p:Resources.save}" />
            <Button Click="cancel" Padding="10,0,10,0" Margin="5" Grid.Column="2" Content="{x:Static p:Resources.cancel}" />
        </Grid>
    </Grid>
    
</Window>
